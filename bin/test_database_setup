#!/usr/bin/env bash
set -euo pipefail

PGPASSWORD="${PG_PASSWORD:-postgres}" psql \
  -h "${PG_HOSTNAME:-localhost}" \
  -U "${PG_USERNAME:-postgres}" \
  -c "CREATE DATABASE ${PG_DATABASE:-xmlephant_test};"
